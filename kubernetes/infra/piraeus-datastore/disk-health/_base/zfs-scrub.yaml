# yaml-language-server: $schema=https://raw.githubusercontent.com/yannh/kubernetes-json-schema/refs/heads/master/master-standalone/cronjob-batch-v1.json
apiVersion: batch/v1
kind: CronJob
metadata:
  name: zfs-scrub
spec:
  schedule: "0 1 1 * *"
  concurrencyPolicy: Forbid
  successfulJobsHistoryLimit: 1
  failedJobsHistoryLimit: 3
  jobTemplate:
    spec:
      backoffLimit: 0
      activeDeadlineSeconds: 46800
      template:
        metadata:
          labels:
            app.kubernetes.io/name: zfs-scrub
        spec:
          restartPolicy: Never
          priorityClassName: platform-low
          nodeSelector:
            kubernetes.io/hostname: c-01
          containers:
            - name: zfs-scrub
              image: harbor.buijnsters.com/library/disk-health:latest@sha256:9375aa8bdc096f44dbdc16a4a14e33707041c72b8dd757adcd6654d6e7f2b42a
              command: ["/bin/sh", "/scripts/zfs-scrub.sh"]
              securityContext:
                privileged: true
              env:
                - name: ZPOOL
                  value: hdd-raidz-thin
                - name: POLL_INTERVAL
                  value: "300"
                - name: TIMEOUT
                  value: "43200"
                - name: NTFY_URL
                  value: "https://ntfy.${SECRET_DOMAIN}/home-disk-health"
                - name: NTFY_TOKEN
                  valueFrom:
                    secretKeyRef:
                      name: disk-health-ntfy
                      key: NTFY_TOKEN
              resources:
                requests:
                  cpu: 10m
                  memory: 64Mi
                limits:
                  memory: 64Mi
              volumeMounts:
                - name: dev
                  mountPath: /dev
                - name: scripts
                  mountPath: /scripts
          volumes:
            - name: dev
              hostPath:
                path: /dev
            - name: scripts
              configMap:
                name: zfs-scrub-script
