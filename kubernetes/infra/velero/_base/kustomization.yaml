# yaml-language-server: $schema=https://json.schemastore.org/kustomization
resources:
  - cm-sc-class-migration.yaml
  - grafana-dashboard.yaml
  - helmrelease.yaml
  #   - netpols.yaml
  - s3-credentials.sops.yaml
  - source.yaml
  - repo-credentials.sops.yaml
configMapGenerator:
  - name: values-velero
    behavior: create
    files:
      - values.yaml=values.yaml
    literals:
      - env-values.yaml=''
configurations:
  - ../../utils/helm/kustomizeconfig.yaml
